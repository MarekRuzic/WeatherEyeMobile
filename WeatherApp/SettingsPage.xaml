<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
             xmlns:nav="clr-namespace:Microsoft.Maui.Controls;assembly=Microsoft.Maui.Controls"
             nav:NavigationPage.HasNavigationBar="False"
             x:Class="WeatherApp.SettingsPage"
             Title="Nastavení"
             BackgroundColor="#F3F7FF">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="22">

            <!-- NADPIS -->
            <Label Text="Nastavení"
                   FontSize="32"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   Margin="0,10,0,20" />

            <!-- SEKCE: NOTIFIKACE -->
            <Frame CornerRadius="14"
                   Padding="20"
                   HasShadow="True"
                   BackgroundColor="White">

                <VerticalStackLayout Spacing="18" HorizontalOptions="Center">

                    <Label Text="Upozornění"
                           FontSize="22"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"/>

                    <!-- Ikona notifikací -->
                    <Image x:Name="NotificationImage"
                           Source="notification_bell_enabled.png"
                           WidthRequest="110"
                           HeightRequest="110"
                           Opacity="0.9" />

                    <!-- Stav + checkbox -->
                    <HorizontalStackLayout Spacing="10" VerticalOptions="Center">
                        <Label Text="Povolit notifikace"
                               FontSize="18"
                               VerticalOptions="Center" />

                        <material:CheckBox  
                            x:Name="CheckBoxNotificationCheck"
                            IsChecked="True"                            
                            CheckChanged="CheckChange_Notification" />
                    </HorizontalStackLayout>

                    <Button Text="Nefungují mi notifikace"
                            HeightRequest="44"
                            CornerRadius="10"
                            StyleClass="ElevatedButton"
                            FontSize="15"
                            TextColor="White"
                            Clicked="NotificationButtonClicked"/>
                </VerticalStackLayout>

            </Frame>

            <!-- Seznam preferencí -->
            <Frame CornerRadius="14"
                   Padding="20"
                   HasShadow="True"
                    BackgroundColor="White">

                <VerticalStackLayout Spacing="16">

                    <Label Text="Preference upozornění"
                            FontSize="20"
                            FontAttributes="Bold"/>

                    <Button Text="Zobrazit moje prefence"
                            HeightRequest="44"
                            CornerRadius="10"
                            StyleClass="ElevatedButton"
                            FontSize="15"
                            TextColor="White"
                            Clicked="AlertPreferenceButtonClicked"/>
                </VerticalStackLayout>
            </Frame>
            
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>